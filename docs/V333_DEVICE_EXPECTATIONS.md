# 3.3.3 device expectations

After installation, the paper toolbar should no longer show the cross-paper drag handle. The app taskbar/launcher icon should own the Settings mission. The right-edge capsule host should expose only visible capsule rows; on retraction its native bounds should contract to the master row and the surrounding native surface should remain transparent.
